apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: applications

# Note: Environments are managed separately via individual GitRepositories
# to avoid namespace conflicts and enable independent deployments

commonLabels:
  app.kubernetes.io/part-of: kaiko-assignment
  app.kubernetes.io/component: applications
