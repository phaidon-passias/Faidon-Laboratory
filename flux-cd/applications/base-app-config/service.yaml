apiVersion: v1
kind: Service
metadata:
  name: app
  namespace: app
  labels:
    app.kubernetes.io/name: app
    app.kubernetes.io/part-of: kaiko-assignment
spec:
  selector:
    app.kubernetes.io/name: app
    app.kubernetes.io/part-of: kaiko-assignment
  ports:
    - port: 80
      targetPort: http

