apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: flux-bootstrap
  namespace: flux-system

resources:
  - sources.yaml
  - applications.yaml
  - infrastructure.yaml

commonLabels:
  app.kubernetes.io/part-of: kaiko-assignment
  app.kubernetes.io/component: flux-bootstrap
