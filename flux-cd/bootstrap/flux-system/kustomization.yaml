apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: flux-system
  namespace: flux-system

resources:
  # This file will be populated by Flux during bootstrap
  # For now, we include a minimal namespace to ensure Flux can bootstrap
  - namespace.yaml
