# This file now only contains the Kustomization for infrastructure
# The GitRepository is managed centrally in flux-cd/gitrepositories.yaml

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: infrastructure
  namespace: flux-system
resources:
  - prometheus-stack/
